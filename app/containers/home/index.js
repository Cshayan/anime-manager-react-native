import React from 'react';
import { ScrollView } from 'react-native';
import { Text } from '../../components';

const Home = () => (
  <ScrollView>
    <Text>HOME</Text>
  </ScrollView>
);

export default Home;
